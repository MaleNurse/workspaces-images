#!/bin/bash

SCRIPT_PATH="$( cd "$(dirname "$0")" ; pwd -P )"
SCRIPT_PATH="$(realpath $SCRIPT_PATH)"
cd $(dirname "${SCRIPT_PATH}")

if [ "$1" == "kasmweb" ]
then
  sudo docker build --no-cache -t kasmweb/jupyter-scipy:dev \
                    -f kasm-jupyter-scipy.Dockerfile .
else
  sudo docker build --no-cache -t doctorwhen/kasm:jupyter-scipy \
                    -f kasm-jupyter-scipy.Dockerfile .
fi
